<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistemacz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ConsultaDenunciaActivity"></activity>
        <activity android:name=".TelaTratamentoFocalActivity" />
        <activity android:name=".TelaDenunciaActivity" />
        <activity android:name=".ListaDenunciaLancadaActivity" />
        <activity android:name=".DenunciaActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaPrincipalActivity" />
        <activity android:name=".ListaImoveisTrabalhadosActivity" />
        <activity android:name=".CadastrarUsuarioActivity" /> <!-- codigo para oculta teclado -->
        <activity
            android:name=".BoletimDiarioActivity"
            android:windowSoftInputMode="stateHidden" />
    </application>

</manifest>